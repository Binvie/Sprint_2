import React from 'react';
import {useNavigate, useParams} from "react-router-dom";

function Detail() {
    const isFruits = useParams();
    const navigate = useNavigate();


    return (
        <>
        </>
    );
}

export default Detail;